<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="umn.ac.id.week10_27299">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WEEK10_27299">
        <service
            android:name="umn.ac.id.CustomBoundService"
            android:enabled="true"
            android:exported="true"></service>
        <service
            android:name=".CustomService"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".SimpleIntentService" />

        <activity android:name=".IntentServiceActivity" />
        <activity android:name=".AsyncTaskLoaderActivity" />
        <activity android:name=".AsyncActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>